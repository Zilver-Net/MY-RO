// ===== rAthena Script ====================================================
//= MY-RO Custom item drop system
// =========================================================================
// = Developed by ZilverNet
//= V. 1.0
//= เพิ่มระบบดรอปไอเทมพิเศษหลังจากฆ่ามอนสเตอร์
//= ดรอปไอเทมพิเศษจะดรอปจากมอนสเตอร์ทุกตัวที่ถูกฆ่า โดยมีโอกาสดรอปตามที่กำหนดไว้ในสคริปต์
//= ไอเทมพิเศษที่ดรอปจะมีทั้งเหรียญทอง, บัตรต่าง ๆ, และไอเทมอื่น ๆ ที่สามารถปรับแต่งได้ตามต้องการ
//= V. 1.1
//= ปรับปรุงโค้ดให้มีประสิทธิภาพมากขึ้น และแก้ไขบัคที่อาจเกิดขึ้นในการดรอปไอเทม
//= เพิ่มการตรวจสอบว่าผู้เล่นยังคงอยู่ในเกมก่อนที่จะทำการดรอปไอเทม
//= V. 1.2
//= ปรับปรุงอัตราการดรอปไอเทมพิเศษให้สมดุลมากขึ้น และเพิ่มไอเทมใหม่ ๆ ที่สามารถดรอปได้
//= V. 1.3
//= แก้ไขปัญหาที่อาจเกิดขึ้นกับมอนสเตอร์บางตัวที่ไม่ดรอปไอเทมพิเศษตามที่กำหนดไว้
//= เพิ่มการดรอปไอเทมพิเศษสำหรับมอนสเตอร์ MVP และ Mini-Boss เพื่อเพิ่มความสนุกและความท้าทายให้กับผู้เล่น
//= V. 2.0 (2026-05-20)
//= ปรับปรุงโค้ดให้สามารถเปิด-ปิดการดรอปไอเทมพิเศษแต่ละประเภทได้อย่างง่ายดายผ่านการตั้งค่าตัวแปรในสคริปต์
//= เปลี่ยนค่าตัวแปรและโครงสร้างของสคริปต์ให้มีรองรับการแก้ไขโอกาสการดรอปและประเภทของไอเทมพิเศษได้อย่างยืดหยุ่นและง่ายขึ้น
//= V. 2.1 (2026-05-22) [Corrected by Gemini 3.5 Flash Extended]
//= แก้ไขโค้ดการดรอป ให้รองรับจำนวนไอเท็มใน array ที่เพิ่มมากขึ้น เพื่อให้โค้ดกระชับ และง่ายต่อการเพิ่มไอเท็มใน array ภายหลัง โดยไม่ต้องยุ่งกับโค้ดการดรอป
//= V. 2.2 (2026-06-07)
//= เพิ่มไอเท็มดรอปสำหรับ Enchant Grade
//= แก้โค้ดการสุ่มลูปดรอปของ MVP และ Mini-Boss ใหม่ ให้โค้ดสามารถทำงานได้ถูกต้อง [Corrected by Gemini 3.5 Flash Extended]
//============================== Script ==============================\\

-	script	DropExtraTreasure	-,{
	
	OnNPCKillEvent:
		if (!playerattached())
			end;
		getunitdata killedgid, .@v;
		.@x = .@v[UMOB_X];
		.@y = .@v[UMOB_Y];
		.@map$ = strcharinfo(3);
		
		if(.IsDropBlessing) {
			.@bles_size = getarraysize(.DropBlessing);
			for(.@i = 2; .@i < .@bles_size; ++.@i) {
				if( rand(.DropRate) <= .DropBlessing[0] ) {
					makeitem .DropBlessing[.@i],rand(1,(.DropBlessing[1]*.AmountMult[2])),.@map$,.@x,.@y;
				}
			}
		}
		if(.IsDropToken) {
			if( rand(.DropRate) <= .DropToken[0] ) {
				makeitem .DropToken[2],rand(1,(.DropToken[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
		if(.IsDropAngpaw) {
			if( rand(.DropRate) <= .DropAngpaw[0] ) {
				makeitem .DropAngpaw[2],rand(1,(.DropAngpaw[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
		if(.IsDropTicket) {
			if( rand(.DropRate) <= .DropTicket[0] ) {
				makeitem .DropTicket[2],rand(1,(.DropTicket[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
		if(.IsDropBox) {
			if( rand(.DropRate) <= .DropBox[0] ) {
				.@box_size = getarraysize(.DropBox);
				makeitem .DropBox[rand(2, .@box_size - 1)], rand(1, (.DropBox[1] * .AmountMult[2])), .@map$, .@x, .@y;
			}
		}
		if(.IsDropRefineMaterial) {
			.@rmat_size = getarraysize(.DropRefineMaterial);
			for(.@i = 2; .@i < .@rmat_size; ++.@i) {
				if( rand(.DropRate) <= .DropRefineMaterial[0] ) {
					makeitem .DropRefineMaterial[.@i], rand(1, (.DropRefineMaterial[1] * .AmountMult[2])), .@map$, .@x, .@y;
				}
			}
		}
		if(.IsDropGradeMaterial) {
			.@gmat_size = getarraysize(.DropGradeMaterial);
			for(.@i = 2; .@i < .@gmat_size; ++.@i) {
				if( rand(.DropRate) <= .DropGradeMaterial[0] ) {
					makeitem .DropGradeMaterial[.@i], rand(1, (.DropGradeMaterial[1] * .AmountMult[2])), .@map$, .@x, .@y;
				}
			}
		}
		if(.IsDropSoul) {
			if( rand(.DropRate) <= .DropSoul[0] ) {
				.@soul_size = getarraysize(.DropSoul);
				makeitem .DropSoul[rand(2, .@soul_size - 1)],rand(1,(.DropSoul[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
		
		//=============================== MVP Drops ==============================\\

		if( getmonsterinfo( killedrid,MOB_MVPEXP ) ){ // MVP Check
			.@mvp_loop = rand(7,10);
			for (.@i = 0; .@i < .@mvp_loop; ++.@i) {
				if(.IsDropVoucher)	{ makeitem .DropVoucher[2],rand(1,(.DropVoucher[1]*.AmountMult[0])),.@map$,.@x,.@y; }
				if(.IsDropCoin) { makeitem .DropCoin[2],rand(1,(.DropCoin[1]*.AmountMult[0])),.@map$,rand(.@x-2,.@x+2),rand(.@y-2,.@y+2); }
				if(.IsDropHP) { makeitem .DropHP[2],rand(1,(.DropHP[1]*.AmountMult[0])),.@map$,rand(.@x-2,.@x+2),rand(.@y-2,.@y+2); }
			}
			end;
		}

		//=============================== Mini-Boss Drops ==============================\\

		if( (getmonsterinfo(killedrid, MOB_MODE) & 102768789) == 102768789 ){ // Mini-Boss Check
			.@mini_loop = rand(0,3);
			for (.@i = 0; .@i < .@mini_loop; ++.@i) {
				if(.IsDropVoucher)	{ makeitem .DropVoucher[3],rand(1,(.DropVoucher[1]*.AmountMult[1])),.@map$,.@x,.@y; }
				if(.IsDropCoin) { makeitem .DropCoin[2],rand(1,(.DropCoin[1]*.AmountMult[1])),.@map$,rand(.@x-1,.@x+1),rand(.@y-1,.@y+1); }
				if(.IsDropHP) { makeitem .DropHP[2],rand(1,(.DropHP[1]*.AmountMult[1])),.@map$,rand(.@x-1,.@x+1),rand(.@y-1,.@y+1); }
			}
			end;
		}

		//=============================== Normal Mobs Drops ==============================\\

		if(.IsDropVoucher) {
			if( rand(.DropRate) <= .DropVoucher[0] ) {
				makeitem .DropVoucher[4],rand(1,(.DropVoucher[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
		if(.IsDropCoin) {
			if( rand(.DropRate) <= .DropCoin[0] ) {
				makeitem .DropCoin[2],rand(1,(.DropCoin[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
		if(.IsDropHP) {
			if( rand(.DropRate) <= .DropHP[0] ) {
				makeitem .DropHP[2],rand(1,(.DropHP[1]*.AmountMult[2])),.@map$,.@x,.@y;
			}
		}
	end;

	OnInit:
		//=============================== Enable Drop Settings ==============================\\
		.IsDropCoin = 1;											// Drop Gold Coin? (1: yes / 0: no)
		.IsDropBlessing = 1;										// Drop Blacksmith Blessing? (1: yes / 0: no)
		.IsDropToken = 0;											// Drop Token Of Siegfried? (1: yes / 0: no)
		.IsDropAngpaw = 1;											// Drop Angpaw? (1: yes / 0: no)
		.IsDropTicket = 0;											// Drop Bossnia Ticket? (1: yes / 0: no)
		.IsDropHP = 1;												// Drop Dried Fish? (1: yes / 0: no)
		.IsDropBox = 0;												// Drop Costume Box? (1: yes / 0: no) Box 1-4
		.IsDropRefineMaterial = 1;									// Drop Crafting Material? (1: yes / 0: no) Enriched Elunium, Enriched Oridecon, HD Carnium, HD Bradium, Enriched Ehterium, Enriched Etherdeocon, HD Etherium, HD Etherdeocon, HD Ether Bradium, HD Ether Carnium
		.IsDropGradeMaterial = 1;									// Drop Crafting Material? (1: yes / 0: no) Ether Aquamarine, Ether Topaz, Ether Amethyst, Ether Amber
		.IsDropSoul = 0;											// Drop Soul? (1: yes / 0: no)
		.IsDropVoucher = 0;											// Drop Monster Voucher? (1: yes / 0: no)
		//=============================== Item Settings ==============================\\
		.DropRate = 10000;											// Base Drop Rate 100.00% **No Change**
		setarray .AmountMult[0],4,2,1;								// Multiplier for amount of items dropped from MVPs*8, Mini-Bosses*4, and regular monsters*1.
		setarray .DropCoin[0],1500,6,33999;							// Drop Rate 15.00%, Max Amount 6, Gold Coin
		setarray .DropBlessing[0],100,3,6635,1000337;				// Drop Rate 1.00%, Max Amount 3, Blacksmith Blessing, Blessed Ether Dust
		setarray .DropToken[0],20,3,7621;							// Drop Rate 0.20%, Max Amount 3, Token Of Siegfried
		setarray .DropAngpaw[0],500,3,33004;						// Drop Rate 5.00%, Max Amount 3, Angpaw
		setarray .DropTicket[0],10,3,6673;							// Drop Rate 0.10%, Max Amount 3, Bossnia Ticket
		setarray .DropHP[0],2000,3,33003;							// Drop Rate 20.00%, Max Amount 3, Dried Fish
		setarray .DropBox[0],20,1,33005,33006,33007,33008;			// Drop Rate 0.20%, Max Amount 1, Costume Box
		setarray .DropRefineMaterial[0],250,3,7619,7620,6225,6226,1000333,1000334,1000335,1000336,1000369,1000371;		// Drop Rate 2.50%, Max Amount 3, Enriched Elunium, Enriched Oridecon, HD Carnium, HD Bradium, Enriched Ehterium, Enriched Etherdeocon, HD Etherium, HD Etherdeocon, HD Ether Bradium, HD Ether Carnium
		setarray .DropGradeMaterial[0],250,3,1000325,1000326,1000327,1000328;		// Drop Rate 2.50%, Max Amount 3, Ether Aquamarine, Ether Topaz, Ether Amethyst, Ether Amber
		setarray .DropSoul[0],100,3,6814,6815,6816,6817,6818,6819;	// Drop Rate 1.00%, Max Amount 3, Soul, Swordman, Merchant, Thief, Mage, Archer, Acolyte
		setarray .DropVoucher[0],2000,6,7615,7616,7617;				// Drop Rate 20.00%, Max Amount 6, MVP Voucher, Miniboss Voucher, Monster Voucher
		end;
}

//=============================== V. 1.3 =========================================
/*
-	script	DropExtraTreasure	-1,{
	
	OnNPCKillEvent:
		.@droprate = 10000;										// Drop Rate 100.00%
		.@dropcoin = 33999;										// Gold Coin
		.@dropblessing = 6635;									// Blacksmith Blessing
		.@dropToken = 7621;										// Token Of Siegfried
		.@dropAngpaw = 33004;									// Angpaw
		.@dropTicket = 6673;									// Bossnia Ticket
		.@drophp = 33003;										// Dried Fish
		setarray .@dropBox[0],33005,33006,33007,33008;			// Costume Box
		.@dropelu = 7619;											// Enriched Elunium
		.@dropbra = 6226;											// HD Bradium
		.@dropori = 7620;											// Enriched Oridecon
		.@dropcar = 6225;											// HD Carnium
		setarray .@dropsoul[0],6814,6815,6816,6817,6818,6819;	// Soul, Swordman, Merchant, Thief, Mage, Archer, Acolyte
		.@dropvouchermvp = 7615;								// MVP Voucher
		.@dropvouchermini = 7616;								// Miniboss Voucher
		.@dropvoucher = 7617;									// Monster Voucher
		
		if (!playerattached())
			end;
		getunitdata killedgid, .@v;
		.@x = .@v[UMOB_X];
		.@y = .@v[UMOB_Y];
		.@z = .@v[UMOB_CLASS];
		.@map$ = strcharinfo(3);
				
		if( getmonsterinfo( killedrid,MOB_MVPEXP ) ){ // MVP Check
			for (set @i,0; @i < rand(7,10); set @i,@i+1) {
				makeitem .@dropvouchermvp,rand(1,5),.@map$,.@x,.@y;
				makeitem .@dropcoin,rand(1,23),.@map$,rand(.@x-2,.@x+2),rand(.@y-2,.@y+2);
				makeitem .@drophp,rand(1,18),.@map$,rand(.@x-2,.@x+2),rand(.@y-2,.@y+2);
			}
			if( rand(.@droprate) < 5000 ) { // Drop Rate 50.00%
				makeitem .@dropelu,rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 5000 ) { // Drop Rate 50.00%
				makeitem .@dropori,rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 5000 ) { // Drop Rate 50.00%
				makeitem .@dropbra,rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 5000 ) { // Drop Rate 50.00%
				makeitem .@dropcar,rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 5000 ) { // Drop Rate 50.00%
				makeitem .@dropbox[rand(6)],rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 2500 ) { // Drop Rate 25.00%
				makeitem .@dropblessing,rand(1,21),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 1000 ) { // Drop Rate 10.00%
				makeitem .@dropsoul[rand(6)],rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 500 ) { // Drop Rate 5.00%
				makeitem .@dropAngpaw,rand(1,10),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 500 ) { // Drop Rate 5.00%
				makeitem .@dropToken,rand(1,3),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 100 ) { // Drop Rate 1.00%
				makeitem .@dropTicket,rand(1,3),.@map$,.@x,.@y;
			}
			end;
		}
		if( getmonsterinfo( killedrid,MOB_MODE ) == 102773397 || getmonsterinfo( killedrid,MOB_MODE ) == 102774421 ){ // Mini-Boss Check
			for (set @i,0; @i < rand(7,10); set @i,@i+1) {
				makeitem .@dropvouchermini,rand(1,5),.@map$,.@x,.@y;
				makeitem .@dropcoin,rand(1,13),.@map$,rand(.@x-1,.@x+1),rand(.@y-1,.@y+1);
				makeitem .@drophp,rand(1,8),.@map$,rand(.@x-1,.@x+1),rand(.@y-1,.@y+1);
			}
		}
		if( rand(.@droprate) < 1500 ) { // Drop Rate 15.00%
			makeitem .@dropcoin,rand(1,6),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 2000 ) { // Drop Rate 20.00%
			makeitem .@dropvoucher,rand(1,6),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 2000 ) { // Drop Rate 20.00%
			makeitem .@drophp,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 100 ) { // Drop Rate 1.00%
			makeitem .@dropblessing,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 250 ) { // Drop Rate 2.50%
			makeitem .@dropelu,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 250 ) { // Drop Rate 2.50%
			makeitem .@dropori,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 250 ) { // Drop Rate 2.50%
			makeitem .@dropbra,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 250 ) { // Drop Rate 2.50%
			makeitem .@dropcar,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 100 ) { // Drop Rate 1.00%
			makeitem .@dropsoul[rand(6)],rand(1,4),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 100 ) { // Drop Rate 1.00%
			makeitem .@dropAngpaw,rand(1,5),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 30 ) { // Drop Rate 0.30%
			makeitem .@dropToken,1,.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 20 ) { // Drop Rate 0.20%
			makeitem .@dropBox[rand(4)],1,.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 10 ) { // Drop Rate 0.10%
			makeitem .@dropTicket,1,.@map$,.@x,.@y;
		}
	end;
}
*/
//=============================== Old code =========================================
/*		if (!playerattached())
			end;
		.@mob_id = killedrid;
		getunitdata killedgid, .@v;
		.@x = .@v[UMOB_X];
		.@y = .@v[UMOB_Y];
		.@map$ = strcharinfo(3);
		
		.@droprate = 5000;
		.@dropcoin = 33999;									// Gold Coin
		.@dropblessing = 6635;								// Blacksmith Blessing
		setarray .@dropelu[0],985,7619,6224;				// Elunium, Enriched Elunium, Bradium
		setarray .@dropori[0],984,7620,6223;				// Oridecon, Enriched Oridecon, Carnium
		.@dropToken = 7621;									// Token Of Siegfried
		.@dropAngpaw = 33004;								// Angpaw
		setarray .@drophp[0],33001,33002,33003;				// Big Bun, Pill, Dried Fish
		setarray .@dropBox[0],33005,33006,33007,33008;		// Costume Box
		.@dropvoucher = 7617;								// Monster Voucher
		.@dropvouchermvp = 7615;							// MVP Voucher
	
		if( getmonsterinfo( killedrid,22 ) ){
			makeitem .@dropvouchermvp,rand(1,5),.@map$,.@x,.@y;
			makeitem .@dropcoin,rand(1,23),.@map$,.@x,.@y;
			makeitem .@drophp,rand(1,18),.@map$,.@x,.@y;
			if( rand(.@droprate) < 500 ) {
				makeitem .@dropori[rand(3)],rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 500 ) {
				makeitem .@dropelu[rand(3)],rand(1,7),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 250 ) {
				makeitem .@dropblessing,rand(1,21),.@map$,.@x,.@y;
			}
			if( rand(.@droprate) < 50 ) {
				makeitem .@dropAngpaw,rand(1,5),.@map$,.@x,.@y;
			}
			end;
		} else
		
		if( rand(.@droprate) < 250 ) {
			makeitem .@dropcoin,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 250 ) {
			makeitem .@dropvoucher,rand(1,6),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 250 ) {
			makeitem .@drophp,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 2 ) {
			makeitem .@dropBox[rand(4)],1,.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 10 ) {
			makeitem .@dropToken,1,.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 100 ) {
			makeitem .@dropblessing,rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 50 ) {
			makeitem .@dropori[rand(3)],rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 50 ) {
			makeitem .@dropelu[rand(3)],rand(1,3),.@map$,.@x,.@y;
		}
		if( rand(.@droprate) < 10 ) {
			makeitem .@dropAngpaw,rand(1,5),.@map$,.@x,.@y;
		}
		end;
*/
//=============================== Aonther code =========================================
/*
		getunitdata(killedgid,.@info);
		if((.@info[UMOB_MODE]&MD_MVP)){ // MVP
			makeitem(33999,1,mapid2name(.@info[UMOB_MAPID]),.@info[UMOB_X],.@info[UMOB_Y]);
		}
		if((.@info[UMOB_MODE]&0x6200000)){ // Mini Boss
			makeitem(33999,1,mapid2name(.@info[UMOB_MAPID]),.@info[UMOB_X],.@info[UMOB_Y]);
		}
		end;
*/
//=============================== Aonther code =========================================
/*
if (!playerattached())
		end;
	.@mob_id = killedrid;
	.@size = getd(".size_" + .@mob_id);
	if (.@size < 1)
		end;
	getunitdata killedgid, .@v;
	.@x = .@v[UMOB_X];
	.@y = .@v[UMOB_Y];
	.@map$ = strcharinfo(3);

	for (.@i = 0; .@i < .@size; .@i++) {
		.@chance = getd(".chance_" + .@mob_id + "[" + .@i + "]");
		if (rand(10000) < .@chance) {
			.@id = getd(".item_id_" + .@mob_id + "[" + .@i + "]");
			.@amt = getd(".item_amount_" + .@mob_id + "[" + .@i + "]");
			while (.@amt > 0) {
				makeitem .@id, 1, .@map$, .@x, .@y;
				.@amt--;
			}
		}
	}
	end;
	
function add_drop {
	.@arg_count = getargcount();
	if (.@arg_count % 4)
		return;

	for (.@i = 0; .@i < .@arg_count; .@i += 4) {
		.@mob_id = getarg(.@i);
		.@size = getd(".size_" + .@mob_id);
		setd ".item_id_" + .@mob_id + "[" + .@size + "]", getarg(.@i+1);	// <item_id>
		setd ".item_amount_" + .@mob_id + "[" + .@size + "]", getarg(.@i+2);	// <item_amount>
		setd ".chance_" + .@mob_id + "[" + .@size + "]", getarg(.@i+3);	// <chance>
		setd ".size_" + .@mob_id, .@size + 1;
	}
	return;
}

OnInit:
	// <mob_id>, <item_id>, <item_amount>, <chance>
	// Chance ========================
	// 10000 	= 100%
	// 1000		= 10%
	// 100		= 1%
	// 99		= 0.99%
	// 50		= 0.50%
	// 1		= 0.01%
	// ===============================
	add_drop( 1002, 607, 2, 10000 );	// poring will drop 2 ygg berries on the floor with 100% chance
	add_drop( 1002, 608, 3, 10000 ); // poring will drop 3 ygg seeds on the floor with 100% chance
	end;
*/